extend layout
block moreLinks
	a(href='/settings/#{name}') Settings
block message
	span Hello #{name}! Learn something you little shit!
block content
	.content_box.span-2.prepend-1.middle_box
		h2 Profile
		p
			| Username: 
			strong #{name}
			br
			| Member Since: 
			strong uh
			
	.content_box.span-5.middle_box
		h2 Active Conversations
		-each message in user.activeSessions
			a(href='/session/#{message[0]}') #{message[1]} #{message[2]}
			br			
	.content_box.span-3.last.middle_box
		h2 New Session
		form(action='/session', method='post')
			input(type='hidden', id='user1', value='dpumford', name='user1')
			input(type='hidden', id='user2', value='fack', name='user2')
			input(type='submit')
