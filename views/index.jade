extend layout
block moreLinks
	a(href='/signup') Do the signup
block message
	span
		| Welcome to 
		strong Pork! 
		|  It's a cool way to learn from other University of Arkansas students. Learn more at our 
		a(href='/about') about
		|  page.
block content
	script(src='/js/jquery.min.js')
	script(src='/js/account_create.js')
	.span-10.last.prepend-1.content_box.middle_box
		h3 Login
		if (name == null)
			form.login(method='post')
				input(type='text', name='name', id='l_username', placeholder='Username')
				br
				input(type='password', name='pass', id='l_password', placeholder='Password')
				br
				input(type='submit', id='l_submit')
		else
			p
				|You've already logged in! View your 
				a(href='/landing/#{name}') Landing Page.
